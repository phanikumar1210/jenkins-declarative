pipeline {
    agent any
    stages {
        stage('First stage'){
            steps {
                sh '''
                    echo "pipeline is successful"
                '''
            }
        }
    }
}